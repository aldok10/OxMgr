# Rust build outputs
/target/

# IDE/editor settings
/.idea/
/.vscode/
__pycache__/

# OS metadata
.DS_Store
Thumbs.db

# Local release/publish artifacts
/release-assets/
/errors/
/apt-repo/
/tap/
/choco-build/
/bench/.cache/
/bench/results/
*.deb
*.nupkg
SHA256SUMS

# npm packaging temporary download/extract outputs
/packaging/npm/oxmgr-v*-*.tar.gz
/packaging/npm/oxmgr-v*-*.zip
/packaging/npm/vendor/*
!/packaging/npm/vendor/.gitkeep

website-link

# AI agent artifacts
/.playwright-mcp/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Session artifacts
sweep-*.png

# Web build artifacts and dependencies
web/node_modules/
web/test-results/
web/tests/oxmgr_home/
phone-descendant-panel.png
test-oxfile.toml
